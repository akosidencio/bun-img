{
  "name": "bun-img-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/bun-img",
    "examples/*"
  ],
  "scripts": {
    "test": "bun test packages/",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "cd packages/bun-img && bun run build",
    "example": "bun examples/bun-server/server.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "astro": "^7.2.9",
    "typescript": "^5.9.0"
  }
}
